# keel 0.12.1 — 框架指令安全更新

CHG-018：agent 可以维护明确属于 keel 的指令；项目自身规则保留。更新器不承担自然语言归属判断，也不增加模型调用或依赖。

## 修复

- `--yes` 自动确认后不再显示 `Proceed? [y/N]`。
- 缺失或不唯一的指令标记保留原文，明确报告哪些文件已更新、`AGENTS.md` 仍待迁移，避免把部分更新说成全部完成。
- 只替换唯一、顺序正确、独占一行的框架标记段，标记外项目文本字节不变；框架中的 `$&` 等内容按原文写入。
- 重复运行且无变化时报告 `already up to date`，不重写文件。
- 现有根指令和 k-impl 说明 agent 如何按已授权范围迁移无标记的旧框架内容，保留项目规则。

## 破坏点

`keel update` 在工具已更新但 `AGENTS.md` 尚需迁移时返回 **2**，不再返回完整成功的 0。脚本调用方应识别这个状态；版本号是已装工具版本，不代表指令已同步。完整应用、无变化或主动取消返回 0（取消不写文件）；写入失败/安装器源文件错误返回 1。

确认仍只作用于预览范围，`--force` 仍仅放行工具降级，不能强行覆盖无明确边界的项目指令。不改变需求/验收记录协议，不修复或绕过用户项目的业务测试。

## 消费项目要做的事

更新本机安装器后，在已接入的项目里运行 `keel update --yes`。若显示 `PENDING AGENTS.md`，让 agent 读取该文件和输出中的安装器源文件，修改明确属于 keel 的内容，保留项目规则原文在标记外，再运行更新。不能简单包住整个混合文件。预览中的本地框架补丁确认覆盖前应先合并或备份。

新的项目用 `keel init`；已有项目不要重新初始化。此版本没有自动给所有本机项目升级，也没有自动发布到 npm 或 GitHub。
