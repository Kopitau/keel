# keel 设计原则 v2：让强模型完成真实功能

- date: 2026-09-05
- status: 工作说明；按本轮优化授权实施，不宣称用户已验收本文
- replaces: DESIGN.md 的运行期指令入口；原文件保留作历史资料
- change: CHG-017
- research: RES-909

## 目标

需求准确、功能有用、选型有据、代码清晰、功能验证充分、迭代容易、说明通俗。流程只服务这些结果，不把产出更多文件当进度。

## 自主性与澄清

用户要求实现时，默认包括必要调研、普通技术决定、实现、修复、验证和清晰交付。只要求审阅、诊断或规划时，不擅自实施。

先查已经存在的事实和授权。只有答案会实质改变行为、范围、成本、安全或不可逆动作时才问；可逆的小决定由模型处理。提问要说明差异，有必要时给推荐。局部阻塞不妨碍其它已授权工作。

同一授权跨阶段、跨会话有效，除非用户改变或撤销。用户明确要求调整某项规则，不应再被这项旧规则要求“先申请修改它”。但授权实施不能伪装成对尚未展示结果的验收。

## 需求、功能和方案

小改动用简短范围与测试义务即可。较大工作把用户目标映射到功能与验收，再拆能独立观察结果的切片。技术调研可以先于细节定稿，用来发现可行性与真正需要用户选择的差异。

重要选型以一手、近期、版本明确的证据为基础，兼顾先进性和成熟度。沿用现有方案也可以是结论；不要求凑齐三个候选，不用新依赖展示“先进”。

代码优先清楚、常规和局部可改；扩展点来自真实变化方向，不来自无限的将来。保留 Node 内置库运行时，不新增流程状态机或平台专用实现。

## 测试、评审和完成

测试证明承诺的外部行为；回归测试尽量证明原缺陷。验收边界和相关失败路径值得测试，文档每句话和实现每个分支不必各配一条测试。

正式验证仍由现有 gate 运行、记录并绑定代码树。相关内容没变时复用证据；改记录不会自动使代码证据过期，改验收则要复核映射。文档检查、替身、人工证据与真实功能测试分别说明。

大方案在当前范围实现后做独立评审；小改动可以做诚实标注的自检。独立评审只拿必要材料，不拿实现聊天。检查实现、可维护性和功能证据；没有可验证影响的偏好只能是建议。

完成是请求的结果已交付；本地通过不等于远端通过，更不等于用户验收。不能为了让形式状态变绿而编造审批、删掉真正的测试义务，或声称未运行的测试通过。

## 记录和指令的职责

根 AGENTS.md 是统一工作契约；技能只补充当前任务的专用机制。当前需求描述要实现什么，当前规划描述怎么验证，历史 DESIGN/DEC 不作为额外必经流程。

确认历史不原地改语义，新版本记录变更。授权范围只记一次；一般实现决定无需 DEC，微小查询无需独立 RES，真实问题才写 ISS。没有问题、经验或被否方案配额。

handoff 保留当前目标、已授权范围、完成与剩余、必要路径。status 只提供结构事实，不替用户分配下一项工作。总结用于方便下一次迭代，不应要求下一位模型重读整个账本。

## 如何继续改进

用真实任务观察：是否重复询问已有答案、是否停在阶段边界、是否扩展到任务之外、是否交付经过验证的功能。发现具体失败再作局部修正，不为假想问题不断加永久规则。
