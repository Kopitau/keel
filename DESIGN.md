<!-- keel-migrated: 2026-08-21 mapping: keel/features/f23-bootstrap/id-map.json -->
# keel — 跨 Agent 项目开发框架 设计规范

> 版本：**v1.0 定稿**（2026-08-21）
> 依据：24 个功能（docs/features.md）× 149 条技术决策（C-01~C-149，全部经对话逐条确认）
> 变更：CHG-001（2026-08-21）——增补 F24 跨平台支持；gate 运行时改为 Node+TS（DEC-149 取代 C-101）。正式记录已迁入 `keel/`（自举，F23）
> 调研支撑：docs/research/（R1 Pocock 谱系 / R2 六平台矩阵 / R3a 框架审计 / R3b 社区实践 / R4 模型版图 / R5 强制层 / R6 kk 审计 / R7 no-mistakes / SUMMARY）
> 语言约定：本规范及所有面向人的记录用中文；框架的 agent 指令用英文（C-124）
> 交付边界：本轮交付设计规范，不含可运行实现（N3）；第 8、9 节为实施轮的建议材料，**不属于已确认内容**
> 文中 (C-xx) 指向确认记录编号，任何一条规则都可回溯到你拍板的那句话

---

## 0. 这份规范怎么读

- 第 1–2 节：这个框架是什么、为什么这样设计（背景，5 分钟）。
- 第 3–4 节：目录布局与生命周期总图（骨架，10 分钟）。
- 第 5 节：**24 项功能的逐条规范**（正文主体，实施的直接依据）。
- 第 6 节：平台适配与执法三档速查。
- 第 7 节：ID 体系与文件速查表。
- 第 8–9 节：实施路线图与技能载体建议（**未确认，仅供实施轮参考**）。
- 附录：功能→决策映射、遗留校准项。

## 1. 定位

keel 是一个**装进项目仓库的开发流程层**：一套带稳定编号的中文记录体系 + 一组跨平台英文技能 + 一个零依赖门禁脚本 + 平台桥接件。它不是 CLI 产品、不接管编排、不绑定任何 agent 厂商。

核心判断（2026-08 调研实证）：前沿模型已能长程执行、自主调研、自我编排，**但作弊与虚假完成率随能力同步恶化**，且上下文压缩后"自主记忆"基本失效。因此框架不教模型怎么思考（阶段内部完全自由），只做四件模型自己做不可靠的事：

1. **留痕**：需求、调研、决策（含暂定项的为什么）、实施过程、问题、经验——写进仓库、带稳定编号、被后续步骤强制消费；
2. **验证**：完成与否由绑定代码树哈希的测试证据 + 独立评审裁决，不由实现者宣告；
3. **审批**：关键控制点（需求基线、技术方案+统一规划、变更、验收、合并）有不可伪造的确认记录；
4. **复用**：一份核心文件六平台可用，任何 harness 任何模型可接手。

设计总原则（用户逐条确认过程中反复出现的三条红线）：

- **模型智能与功能实现优先，省 token 不以牺牲两者为代价**（C-69 及 F5 全组）；
- **确认过的工件不可变**：迭代=新建版本文件+重索引，历史即审计链（C-24/C-63）；
- **记录必须被消费**：每类记录都规定"谁在哪一步必读"，写完没人看的记录不设。

## 2. 总体架构

### 2.1 四个组成部分

```
┌──────────────────────────────────────────────────────────┐
│ ① 指令入口  AGENTS.md（英文 ≤150 行，地图式指针）           │
│    桥：CLAUDE.md = "@AGENTS.md" 一行（唯一需要桥的平台）     │
├──────────────────────────────────────────────────────────┤
│ ② 技能层    .agents/skills/（Agent Skills 开放标准，        │
│    五家原生读取）＋ .claude/skills/ 镜像（脚本生成）          │
├──────────────────────────────────────────────────────────┤
│ ③ 记录层    keel/（可见目录：需求/规划/功能/决策/问题/       │
│    变更/开源登记/经验/审批/日志/交接/总览/配置）              │
│    ＋ ~/.keel/knowledge/（用户级跨项目知识库）                │
├──────────────────────────────────────────────────────────┤
│ ④ 强制层    tools/gate/（Node + TypeScript，仅内置模块）     │
│    一份实现五处调用：状态/门禁/git hook/平台钩子/CI           │
└──────────────────────────────────────────────────────────┘
```

### 2.2 五层强制模型（C-100）

```
L0 指令（AGENTS.md/技能）      劝导性——塑造行为，不算执法
L1 平台钩子（六家薄壳）         提前反馈，可绕（有公开实证）
L2 git hooks（.githooks/）     提交/推送时快速拦截，可绕
L3 CI 复算                    ★唯一权威：同一脚本全量重算
L4 人工审批（哈希记录）         关键控制点
```

L1/L2 失效不影响正确性，只影响反馈速度；一切以 L3 为准。

### 2.3 执法三档（C-48/C-104，诚实标注）

| 档 | 场景 | 形态 |
|---|------|------|
| 完整档 | GitHub | 分支保护 + required check（gate 复算）+ 审批目录 CODEOWNERS |
| 降级档 | Gitee | 保护分支 + 尽量配 CI 复算 + 哈希审批文件 |
| 本地档 | 无远端平台 | git hooks + 合并前强制跑 gate + 哈希审批文件 + 合并说明存档；文档注明"防呆不防恶" |

## 3. 目录布局

```
项目根/
├── AGENTS.md                      # 英文根指令 ≤150 行（C-118）
├── CLAUDE.md                      # 一行 "@AGENTS.md"（C-94）
├── CONTEXT.md                     # 术语表，英文词条+可附中文注释，懒创建（C-125）
├── .agents/skills/k-*/SKILL.md    # 技能唯一事实源（C-94/95）
├── .claude/skills/                # 镜像，脚本生成勿手改（C-94）
├── .githooks/                     # sh 薄壳 → gate（C-102）
├── .github/workflows/…            # GitHub 档 CI（C-104；Gitee/本地档见 §6）
├── tools/gate/                    # 门禁实现 Node+TS（DEC-149 取代 C-101）
│   └── gate.sh / gate.ps1         # 双启动器，自动探测运行时（DEC-146）
└── keel/                          # 记录层（可见目录，C-99）
    ├── config.json                # 集中配置（C-129）
    ├── OVERVIEW.md                # 项目总览，活文档（C-53/54）
    ├── handoff.md                 # 最新交接（C-70）
    ├── requirements/
    │   ├── INDEX.md               # 脚本生成，唯一当前版指向（C-63）
    │   └── v1.md, v2.md…          # 需求书版本（REQ 条目集，不可变，C-63）
    ├── plan/
    │   ├── INDEX.md               # 唯一当前指向，缺失/双指报错（C-24）
    │   └── overview-vN.md         # 统一规划总览 ~100 行（C-24）
    ├── features/<功能名>/
    │   ├── plan/vN.md             # 该功能规划 30~60 行，不可变（C-24）
    │   ├── worklog.md             # 实施记录，追加式（C-20/C-77）
    │   └── summary.md             # 功能总结（C-52）
    ├── research/RES-###-*.md      # 调研报告（C-09）
    ├── decisions/DEC-###-*.md     # 决策账本，一决策一文件（C-13）
    ├── issues/ISS-###-*.md        # 问题日志（C-58）
    ├── changes/CHG-###-*.md       # 变更单（C-64）
    ├── oss/OSS-###-*.md           # 开源复用登记（C-88）
    ├── lessons/LES-###-*.md       # 项目经验（C-79；候选在 worklog 打标签，C-77）
    ├── approvals/APR-###.md       # 审批记录，人类身份提交（C-106/107）
    └── journal/<开发者>/*.md       # 会话日志，按人分目录（C-71）

用户目录/
└── ~/.keel/knowledge/KLES-###-*.md + INDEX   # 跨项目知识库（C-82）
```

通用规则：**一物一文件**（决策/问题/变更/OSS/经验——并行零冲突）；**索引皆生成**（脚本重建、禁止手写，C-117）；**记录目录永不自动注入**任何平台上下文（C-120）。

## 4. 生命周期总图

```
需求访谈(F1) ─基线确认─▶ 强制调研(F2) ─▶ 方案逐条确认(F3)
      ─▶ 统一实施规划(F4) ═你一次确认═▶ 【自主实施区】
            循环每个功能：实现+落盘(F4/F5) → 测试与证据(F6)
            → 独立评审(F7) → 你验收(F7) → 合并(F8) → 复盘总结(F9)
横切：问题闭环(F10)  变更管理(F11)  会话交接(F12)  摩擦沉淀(F13)
      跨项目知识(F14)  开源登记(F15)
入口特例：旧框架项目（Trellis/Superpowers）先走 F22 迁移 →
      迁移初稿经确认后接入上图正常流程
```

人工确认点只有五类：**需求基线、方案+统一规划（捆绑一次）、变更单、功能验收、合并**（外加规划外边界被触碰时的升级问询，C-21）。其余全部 agent 自主推进并落盘。

六道门禁（C-103，gate 裁决、CI 权威）：

| 门禁 | 卡什么 | 依据 |
|------|--------|------|
| G-需求 | 基线前无未决澄清标记；未解分支已落"未决问题"节 | C-05 |
| G-调研 | 每个重大决策指向调研报告或书面豁免 | C-10 |
| G-规划 | 版本索引唯一；接口耦合表存在 | C-24 |
| G-完成 | 证据树哈希匹配 + 追溯全绿 + **实际重跑测试对账** | C-33 |
| G-合并 | 验收记录 + 证据有效 + 追溯全绿 + 无未闭环 blocking 问题 | C-45 |
| G-复盘 | 总结齐全 + 总览已更新 + 问题全闭环 + 暂定销项有记录 | C-56 |

门禁输出：通过/警告/不通过 + 逐项原因 + 修复指引；**警告放行必须在 worklog 记一行理由**（C-103）。

## 5. 二十四项功能规范

### F1 需求访谈与记录（C-02~C-07）

1. **按轮批量提问**：每轮列出当前所有可问的问题（编号 + 每题附推荐答案），用户一次回复；有依赖的问题留下一轮；用户可随时切换"一次一问"（C-02）。
2. **事实归 agent，决策归人**：代码/文档/网络可查的事实自查，不许问用户；只把取舍判断交给用户（C-03）。
3. **需求条目**：需求书单文档（版本化见 F11），条目 = REQ-编号 + 状态 + 来源 + 描述 + 验收标准 + 边界与反例 + 非目标（C-04）。
4. **验收标准记法**：Given/When/Then 为主，简单条目允许 checklist 简写（C-07）。
5. **禁猜**：模糊处强制 `[NEEDS-CLARIFICATION: 具体问题]`；未解分支必须写进需求书"未决问题"节，不许只留在对话里（C-05）。
6. **基线确认**：用户整体点头一次 = 需求基线（重确认，落 APR 哈希）；大功能/新项目点头前由**未参与访谈的新上下文 agent** 独立挑一遍遗漏与矛盾，小功能 agent 自检（C-06）。

### F2 强制调研（C-08~C-12）

1. **三档分级**：深度（新架构/关键依赖/数据模型/安全/公共接口/跨平台/高不确定 → ≥2-3 候选对比 + 一手引用 + 找反例 + 必要时 spike）/ 标准（本地证据 + 官方文档 + ≥1 替代方案）/ 本地（可不外查但记判断依据）；档位 agent 自选并声明，统一规划确认时用户可要求升档（C-08）。
2. **报告格式**：一问题一份 RES 文件：调研问题 → 检索范围 → 候选对比 → 逐项证据（引用+访问日期，`[未核实]` 显式标注）→ 结论（决定/理由/备选三段）→ 剩余不确定性；**必须人类易读、通俗易懂**（C-09）。
3. **门禁**：重大决策必须指向调研报告，否则决策记录写豁免理由；脚本查引用存在性（C-10）。
4. **开源衔接**：调研选用开源项目 → 强制建 OSS 登记条目（C-11）。
5. **离线不豁免**：用带来源/版本/日期的离线资料替代或明确停下，禁止凭印象选型（C-12）。

### F3 技术方案逐条确认 + 决策账本（C-13~C-18）

1. **一决策一文件**：DEC-编号：机器可读头（编号/标题/状态/日期/关联功能/调研引用）+ 通俗中文正文（问题 → 选项对比 → 推荐理由 → **用户决定原话** → 影响）（C-13）。
2. **状态机**：proposed → confirmed / provisional（必须带理由+复核触发条件）/ deferred；推翻 → superseded（必须指向新决策）；脚本校验迁移合法与字段齐全（C-14）。
3. **按轮呈报、当场写入**，不积压到会话尾（C-15）。
4. **重大决策**：同时满足三门槛（难逆转 + 无上下文会意外 + 真权衡）加写"后果与复审条款"并打 adr 标记；不另设 ADR 目录（C-16）。
5. **低层决定**：实现期低层可逆选择 agent 自主；非显然的记轻量实现决定进 worklog；触碰高层边界自动升级（C-17）。
6. **暂定防遗忘**：状态脚本常驻显示暂定计数；复盘逐条销项（C-18）。

### F4 统一实施规划（C-19~C-24）

1. **总-分结构 + 版本化不可变**（C-24）：`plan/INDEX.md`（脚本生成，唯一当前指向）+ `plan/overview-vN.md` 总览（~100 行：功能清单/**功能间接口与耦合表**/实施顺序与依赖）+ `features/<功能>/plan/vN.md`（30~60 行：范围/测试义务/内部步骤/预计触碰文件范围）。确认即冻结；迭代经变更批准后**新建完整版本文件**并重索引；小项目（≤3 功能）可合并书写。
2. **捆绑一次确认**：规划与技术方案基线一起，用户一次点头；确认记录含内容哈希（C-19）。
3. **实施落盘**：每功能 worklog 随做随记（内部分解/进度/非显然选择理由/问题链接），追加式不重写历史（C-20）。
4. **边界升级**：触碰已确认接口/数据契约、需求边界、规划外重大依赖、已确认测试义务 → 必须停下问用户；其余全自主（C-21）。
5. **规划纠错**：本功能内部小调整记实现决定即可；影响接口或其他功能 → 走变更单更新规划后再继续（C-22）。
6. **中途新增功能**：走变更单，含实施规划补充，随变更确认（C-23）。

### F5 框架开销控制（C-25~C-30、C-69）

1. **零 token 环节**：状态查询/门禁裁决/证据核验/追溯矩阵/索引生成/编号分配/骨架创建/技能镜像 = 确定性脚本；模型只做需要智能的环节（C-25）。
2. **常驻装载 ≤10KB**（根指令+技能名录；初值，试点校准）（C-26）。
3. **开场三跳**：状态脚本输出 → 交接文件 → 当前功能小规划+worklog；再往外按导航建议自由取用**不设墙**（C-27）。
4. **写入轻量化**：骨架脚本生成、模型填正文；模板懒创建；骨架是便利不是强制（C-28）。
5. **文档分层**：根指令只做地图，细节从属文件按需读（C-29）。
6. **子代理检索**（可选优化）：大检索派子代理、主上下文收结论；无子代理平台顺序执行（C-30）。
7. **记录写作降耗**（C-69）：长文初稿（调研成文/总结初稿/规划初稿）由"记录员"= **同主力模型 + medium 思考强度**扩写（不降模型等级；砍的是深推理开销），主模型过目后落盘；**短记录一律内联直接写**（绕道更贵——总 token 节省仅当砍掉的思考量 > 简报+回读开销，只对长文成立）；全部记录**引用而非复制**。

### F6 测试与完成证据（C-31~C-38）

1. **分级**：核心（业务逻辑/数据处理/对外接口/算法）先测后码；辅助（胶水/包装/配置）实现后必补；豁免（探索 notebook/一次性脚本/纯文档）留痕（C-31）。
2. **追溯**：测试带需求编号标记（pytest marker / 测试名约定）；脚本生成追溯矩阵，**按验收标准逐条对账**，零引用=门禁不过（C-32）。
3. **证据协议**：运行证据小 JSON = 命令/退出码/起止时间/git 提交+**代码树哈希**/报告哈希/计数/需求覆盖映射/输出尾 2KB/执行者（harness/模型/会话）；**树哈希不匹配旧证据自动作废**；门禁重跑对账拦谎报（C-33）。
4. **防篡改**：测试文件改动评审单独过目；skip/删除测试须引用决策或问题编号；禁同义反复测试（C-34）。
5. **红灯证据**：核心级新测试先红后绿留红灯证据；修 bug 回归测试"无补丁挂、有补丁过"；辅助级不要求（C-35）。
6. **v1 不设覆盖率门与变异测试**（覆盖率门逼出凑数测试；变异测试重且 Windows 差，列 v2）；质量靠追溯全覆盖+红灯证据+独立评审三件事（C-36）。
7. **功能级验收测试（必须）**：每功能按 GWT 验收标准写端到端验收测试（Web=Playwright 实点 / 管道=样例数据全流程 / CLI=实际调用 / 服务=集成调 API）；**每条验收标准 ≥1 条可运行验收测试**；不全绿不得进验收（C-37）。
8. **功能间联动测试（必须）**：以统一规划接口耦合表为义务来源，每条已声明接口 ≥1 条契约/集成测试；联动任一方变化，树哈希使旧证据作废须重验（C-38）。

### F7 独立评审与人工验收（C-39~C-44）

1. **隔离**：未参与实现的全新上下文评审；只给 diff/小规划+测试义务/需求条目/证据/worklog 摘要，**不给实现聊天记录**（C-39）。
2. **异构复审 = 可选配置项**：默认同源新上下文；项目配置可开"另一家 harness/模型复审"，推荐核心功能开但不强制（C-40）。
3. **两轴**：规格轴逐条核对验收标准与测试真覆盖（含篡改/同义反复检查）；工程轴**只报影响正确性/需求达成的问题**——明确防吹毛求疵；评审者只报不改（C-41）。
4. **修复回路**：blocking 回修后由新一轮干净上下文复审（旧结论只当线索，修复代码按同等对抗标准重审）；advisory 进待办不阻塞（C-42）。
5. **验收材料五样**：需求逐条对照 / 验收+联动测试结果 / 证据指针 / 评审结论与遗留 / 已知限制与暂定项；点头=验收（落哈希）（C-43）。
6. **验收 ≠ 合并**（C-44）。

### F8 合并/集成车道（C-45~C-50）

1. **前置四条件**：验收记录 + 证据树哈希有效 + 追溯全绿 + 无未闭环 blocking 问题；本地先算，**CI 复算是权威**（C-45）。
2. **合并方式**：一功能一分支走 PR；PR 描述脚本自动生成；主干禁直推 + required check + 人工 approve；**squash 合并**（C-46）。
3. **no-mistakes = 可选组件默认不装**：装则其对抗性 review 可充当 F7 实现、gate 配进其命令位；不装流程完整；权威仍是 CI；Gitee 无其 PR/CI 步（C-47）。
4. **执法三档**见 §2.3（C-48）。
5. **合并后**：成功 → 触发复盘 → 清理分支/worktree；失败 → 开问题回修（C-49）。
6. **联动重验**：先合入者使在途功能基线变旧，状态脚本提示 rebase+联动重跑，树哈希自动作废受影响证据（C-50）。

### F9 复盘与功能级总结（C-51~C-56）

1. 合并成功后立即复盘；可随时手动触发项目级复盘（C-51）。
2. **功能总结** `summary.md` 固定节：做了什么/为什么（对照需求）→ **技术路线说明**（架构与选型、数据流、为什么这么走——引用决策与调研编号）→ 关键决策与被否方案 → 测试与证据指针 → 遗留债务与已知限制。通俗中文，不含花费（C-52）。
3. **项目总览** `OVERVIEW.md`：项目是什么/整体技术路线图/能力清单（链功能总结）/在途功能/风险与暂定计数；每次复盘更新；**开场协议必读**（C-53）。
4. 总览是活文档原地更新——版本化边界 = "是否绑定过用户确认"（C-54）。
5. **复盘三销项**：暂定决策销项 + 问题闭环核查 + 摩擦候选核实（C-55）。
6. **完成门禁**：总结齐全 + 总览更新时间 ≥ 合并时间 + 问题全闭环 + 销项有记录，缺一不许标完成（C-56）。

### F10 问题日志与防复发闭环（C-57~C-62）

1. **两级门槛**：异常先 worklog 记一行；满足条件（影响行为/致失败/需调查/可能复发/致返工/评审验收发现）才升正式 ISS 条目（C-57）。
2. **条目**：机器头（编号/状态/防线类型/防线指针）+ 正文：现象（**含复现命令**）→根因→修复→**为何未被更早发现**→闭环选择与理由（C-58）。
3. **闭环阶梯**：回归测试 → lint → 门禁/hook → 项目规则（**条数封顶超限蒸馏**）→ 决策修订 → 显式不修+理由；可能复发的不许只留档；**选了哪级、为什么不用更高级必须记录**（C-59）。
4. **修 bug 纪律**：无复现不修；回归测试先行；修 3 次不成熔断升级架构讨论；**复现命令/假设与验证/每次尝试/熔断全过程必须落盘**（C-60）。
5. **重复加重**：同指纹再现必须解释上次防线为何失效并升一级；同指纹同功能 ≥3 次强制触发经验候选（C-61）。
6. **防线指针脚本校验存在性**——说加回归测试就必须真有那个文件（C-62）。

### F11 需求变更管理（C-63~C-68）

1. 需求书版本化：`requirements/vN.md + INDEX`，基线确认即冻结（C-63）。
2. 变更单 CHG：动机 → 增/改/删逐条 → 影响评估（追溯矩阵辅助）→ 批准 → 出新版重索引（C-64）。
3. 批准联动：受影响决策标复核/作废、功能规划出新版、测试义务更新、旧证据作废重验（C-65）。
4. **硬冻结**：绕过变更单改当前版 = CI 不过；紧急小改也走变更单（C-66）。
5. **澄清通道**：不改行为/范围/验收语义的纯文字修订出"澄清版"，免影响评估免重新确认；判据 = 验收标准含义变没变（C-67）。
6. 版本文件+变更单 = 完整演化审计链（C-68）。

### F12 会话交接/断点续传（C-70~C-75）

1. handoff.md：做了什么/为什么、当前功能与阶段、下一步、未决问题、该读文件导航；收尾或上下文将满时更新（C-70）。
2. journal 按开发者分目录，每会话一篇（C-71）。
3. 开场协议 = 三跳 + OVERVIEW 前置；状态脚本输出含上次交接路径/未决问题数/暂定计数（C-72）。
4. **交接完全不依赖平台会话格式**——六家互不兼容；记录体系即全部载体，任何 harness 任何模型可接手（C-73）。
5. hook 双保险可选（CC/Codex/Grok 配会话启动提醒钩子），不构成依赖（C-74）。
6. worklog 随做随记保证任意中断可恢复（C-75）。

### F13 摩擦沉淀（经验观察者）（C-76~C-81）

1. 五类信号：用户纠正 / 同指纹重复 / 旧防线失效 / 评审模式性发现 / 知识缺口（C-76）。
2. **候选零仪式**：worklog 一行打标签（`#经验候选` + 类型 + 一句话），脚本汇集（C-77）。
3. 不打断/不重试/不因数量自动升级；同指纹同功能 ≥3 次强制升级（C-78）。
4. 复盘核实：属实 → LES 正式经验（现象/教训/适用边界/反例）+ 定去向；不实 → 丢弃留一行原因（C-79）。
5. 消费点：脚本按功能/标签在规划、评审环节提示相关经验（C-80）。
6. 与问题日志分工：问题=具体故障闭环；经验=跨问题模式教训；互不替代（C-81）。

### F14 跨项目知识库（C-82~C-87）

1. `~/.keel/knowledge/` 跟人走；KLES 一条一文件 + 脚本索引；不入项目仓库；团队共享=该目录自建 git 仓库（C-82）。
2. 提升手动为主 + 复盘时脚本建议清单；**必须脱敏泛化**；不自动同步（C-83）。
3. 开场只注入索引摘要几行；条目按需拉取（新项目初始化/调研/规划评审导航）（C-84）。
4. 跨项目知识只是候选；成为某项目正式规则须走该项目确认，注明来源（C-85）。
5. 每条带最后验证日期+适用前提；失效标过期/删除；条数 ~100 封顶提醒蒸馏（C-86）。
6. keel 知识库 = 平台无关唯一事实源；平台自带记忆仅个人便利（C-87）。

### F15 开源复用登记与追踪（C-88~C-93）

1. OSS 一项目一文件：机器头（仓库/**精确版本或 commit**/许可证/复用方式/关联功能/复查周期与日期）+ 正文（复用点/本地差异/**追踪计划**）（C-88）。
2. 触发：调研选用强制建；新增**直接依赖**须登记（验收门禁核对依赖清单 vs 登记表，缺则不过；传递依赖不要求）；借鉴改造代码建条目+代码注释来源（C-89）。
3. 复查默认 28 天可调；**只读**对比上游；结论 ∈ 无变化/关注/建议调研/建议变更；不自动改代码，升级走变更（C-90）。
4. 状态脚本报到期数；agent 会话内联网批量执行；离线顺延留记录（C-91）。
5. 许可证合规：GPL/AGPL 类借鉴改造升级提醒用户决策；上游改协议=建议变更级（C-92）。
6. 功能总结引用 OSS 条目；OVERVIEW 加"主要外部依赖"节（C-93）。

### F16 六平台适配（C-94~C-99）

1. **单一事实源+薄桥**：AGENTS.md + .agents/skills/ → Codex/OpenCode/Pi/Grok/dsh 五家原生零适配；Claude Code 桥 = 一行 @AGENTS.md + 技能镜像（Windows 复制不软链）。技能格式是**同一开放标准**，唯一落地适配 = 触发词措辞逐家实测校准（C-94）。
2. 技能只用标准字段（名=目录名≤64、描述≤1024 关键词前置），零私有字段不嵌套（C-95）。
3. **主力档五家：Claude Code / Codex / OpenCode / Grok Build / DeepSeek Harness**（逐一实测技能触发与门禁）；兼容档：Pi（冒烟）。dsh 的 Windows 限制如实标注（Windows 用 dsh 走 WSL）（C-96）。
4. 平台钩子 = 转调 gate 的薄壳（CC/GK 共用配置、Codex 同款、OC 插件、Pi 扩展）；任何一家失效不影响正确性（C-97）。
5. headless 配方收录一页备查；CI 只依赖 gate 与 git（C-98）。
6. **命名正式确认：keel / keel/ 目录 / k-* 技能前缀**（C-99）。

### F17 门禁强制层（C-100~C-105）

见 §2.2/§4；补充：gate = **Node + TypeScript，直接运行 .ts（原生类型剥离，零构建）、仅用内置模块零依赖**，一份实现五处调用（DEC-149 取代 C-101；实证见 RES-901：六个目标 harness 中五个经 npm 分发，Node 必然存在；启动开销与 Python 差异被一次 git 调用淹没）；`.githooks/` + core.hooksPath，prepare-commit-msg 注入尾注不受 `--no-verify` 抑制（C-102）；反绕过拦截清单 + gate 自带测试 + **模型大升级后复审门禁清单删过时约束**（C-105）。

### F18 审批记录机制（C-106~C-111）

1. APR = 工件路径+版本+**内容哈希**+批准人+日期+范围；哈希绑**规范性内容**，错字修订不作废、语义变才重批（C-106）。
2. 审批文件必须在**人类 git 身份**的提交里（agent 身份清单入配置，CI 校验作者∉清单）；agent 起草、用户 `gate approve` 以本人身份提交（C-107）。
3. GitHub 档：approvals/ 进 CODEOWNERS（C-108）。
4. 合并审批直接用平台 PR approval；本地档用哈希审批文件（C-109）。
5. **两级确认**：轻确认=对话同意当场记录继续干；重确认=冻结级工件落 APR（C-110）。
6. 信任模型明说：防误记谎报，不防人为恶意；不强制签名不建密码学身份（C-111）。

### F19 多人团队 + 多 agent 并行（C-112~C-117）

1. 一功能一分支一 worktree（`keel/F-编号-短名`），脚本统一建/清，六平台通吃；同功能默认单写者（C-112）。
2. 认领 = assignee 字段 + 建分支；脚本检测双认领报警；换手须释放或显式接管留记录；乐观并发不搞心跳锁（C-113）。
3. 并行依据 = 接口耦合表 + 预计触碰文件范围；重叠 → 串行或接口先行；认领时脚本自动查重叠（C-114）。
4. 提交尾注：功能/开发者/agent 平台模型/会话（C-115）。
5. 先合者赢；后合者 rebase+联动重验；主干 squash；PR 标题带功能编号（C-116）。
6. 共享记录防冲突：一物一文件 + 追加式 + 生成式索引；OVERVIEW 只在复盘时由完成方更新（C-117）。

### F20 上下文预算控制（C-118~C-123）

预算初值：AGENTS.md ≤150 行且链 ≤32KiB（Codex 静默截断硬线）/ 技能 ≤500 行、描述 ≤1024 字符 / 自动装载 ≤10KB（C-118）。CI 机检：超软预算=警告，**超平台硬限=不通过**（C-119）。记录不注入负面清单（C-120）。技能十几个封顶（C-121）。平台硬限档案一页、数字有出处（C-122）。超限处理：内容下沉、提预算须改配置+记决策（C-123）。

### F21 语言与项目类型配置（C-124~C-129）

1. 英文：AGENTS.md/技能/工作流契约/记录字段名/脚本机器输出；中文：各类记录正文；记录 = 字段名英文 + 正文中文（C-124）。
2. CONTEXT.md 术语表：英文词条为主可附中文注释，只收术语与禁用近义词（C-125）。
3. 测试画像：Python 后端/CLI = pytest；DS/ML = pytest + nbmake/nbval + 数据校验 + 评估指标 JSON 入证据；TS/JS = Vitest/Jest + Playwright；其他 = junit 兼容（C-126）。
4. notebook：探索免测试记豁免；沉淀 = 迁出模块 + 补核心级测试；**正式管道不允许只活在 notebook 的生产逻辑**（C-127）。
5. 多语言项目多画像并存，门禁按触碰范围选命令（C-128）。
6. config.json 集中所有开关：画像/测试命令/身份清单/平台清单/预算/可选组件（no-mistakes、异构评审、记录员降耗）（C-129）。

### F22 既有框架迁移（C-130~C-136）

1. **映射表式转换**（C-130）：每个源框架一张映射表。**Trellis**：spec/PRD → 需求书初稿（逐条拆 REQ）；design/research → 调研报告 + 决策初稿（无理由的标"暂定·需补理由"）；已完成 tasks → 功能目录 + 追溯性总结初稿；在途 tasks → 功能 + 规划初稿；journal/工作区 → 日志归档 + 生成交接文件；workflow.md 自定义规则 → 与 keel 规则逐条比对，冲突列给用户裁决。**Superpowers**：docs/plans → 对应功能的规划/总结初稿；头脑风暴产物 → 需求素材。v1 支持这两家，映射表可扩展（日后加 spec-kit/BMAD 只需加表）。
2. **初稿不自动成基线**（C-131）：迁移产物一律标"迁移初稿（未确认）"，走 keel 正常确认流程（用户过目 → 点头 → 落哈希）才成基线；迁移工具无权替用户确认。
3. **只读源、不破坏**（C-132）：只读取旧框架目录不删不改；完成后原地打"已迁移"标记，删除由用户核对后另行确认。
4. **迁移报告**（C-133）：映射了什么→哪里 / 没映射什么、为什么 / 冲突项 / 待确认清单；通俗中文。
5. **双框架互斥**（C-134）：迁移完成经用户确认后停用/移除旧框架的技能、钩子与指令注入；旧 CLAUDE.md/AGENTS.md 中仍有价值的规则合并进 keel 的 AGENTS.md（守 ≤150 行预算，超则下沉）。
6. **执行载体**（C-135）：语义映射由 k-migrate 技能驱动模型执行；gate 只做机械部分（编号/骨架/索引/报告模板）；大项目可分批迁（先在途后历史）。
7. **非结构化文档挖掘**（C-136）：除框架目录外，挖掘项目 docs/、README、根目录散落 .md 等不规范内容：需求素材 → REQ 初稿；规划设计 → 规划或决策初稿；规则约定 → 项目规则候选；过时/矛盾内容 → 迁移报告存疑清单（标来源与最后修改时间，**不盲信陈旧文档**）；无法归类 → 报告列出。同样遵守初稿不自动成基线、只读不破坏。

### F23 框架自举（自己管理自己）（C-137~C-142）

1. **自举两段式**（C-137）：设计阶段产物（features.md/decisions.md/research//DESIGN.md）在 W1 按 F22 方式迁移成 keel 自身标准记录，**保留原编号映射、不重写历史**；骨架生成后 keel 仓库立即用自己的目录/门禁/技能管理后续全部开发。
2. **上一版管下一版**（C-138）：改 gate/技能/模板的提交由 CI 检出**主干现行版 gate** 裁决，合并后新版才生效——防"正在改的门禁自己批准自己"；gate 改动带自测试并通过全部既有门禁用例。
3. **框架测试适配**（C-139，DEC-152 修订）：gate 自身 = `node:test`（JUnit reporter 对接证据）；技能 = 无歧义测试（未读过的模型执行一遍）+ 五平台触发实测；模板/记录格式 = 校验测试；框架的功能级验收测试 = 冒烟样例项目全流程走通。消费项目测试画像仍按 C-126。
4. **演化走自己流程**（C-140）：后续功能/改动走 F1~F11 全流程；破坏性变更（目录协议/字段/门禁语义）须 adr 决策 + 变更单，release 说明列明。
5. **消费项目升级从简**（C-141）：git tag 出版本；升级 = 脚本辅助拉新版文件（先列将覆盖清单）+ 用户确认执行；v1 不做自动升级与版本锁。
6. **本会话 = 第一批账本**（C-142）：本轮对话的每次确认即 keel 项目的第一批正式审批记录；E:\program\en 的四类产物为需求基线与决策账本来源。

### F24 跨平台支持（Windows + macOS + Linux）（DEC-143~DEC-148，CHG-001）

1. **正式支持矩阵**（DEC-143）：Windows / macOS = 开发机一等公民；**Linux = CI 运行环境一等公民**（CI 复算是唯一权威门禁，本就跑在 Linux runner 上）。三平台门禁结论必须一致。
2. **哈希一致性**（DEC-144，本组风险最高）：CRLF/LF 差异会让同一文件在不同平台算出不同哈希，直接击穿审批哈希（C-106）与证据哈希（C-33）——表现为"Mac 上确认的基线，Windows 上显示被篡改"。双保险：`.gitattributes` 强制 `* text=auto eol=lf`，且**哈希一律在规范化内容（LF、UTF-8 无 BOM）上计算**，不直接哈希磁盘字节。
3. **路径与大小写**（DEC-145）：一律用运行时路径 API，禁止硬编码分隔符；ID 与文件名统一小写连字符；**CI 增加大小写冲突检查**（macOS/Windows 不敏感、Linux 敏感，同名不同大小写会在 CI 炸）。
4. **启动器与 hooks**（DEC-146）：`gate.sh`（macOS/Linux）+ `gate.ps1`（Windows），自动探测运行时；git hooks 一律 `#!/bin/sh` 薄壳（macOS/Linux 原生、Windows 走 Git Bash）。
5. **镜像统一复制**（DEC-147）：C-94 的"Windows 用复制不软链"扩展为**三平台一律复制**——镜像是脚本可重建产物，统一行为消除平台相关 symlink 的坑。
6. **跨平台一致性测试**（DEC-148）：同一组 fixture 在 Windows 与 macOS 上必须产出**相同的门禁结论、规范化哈希、追溯矩阵**；CI 矩阵加 macOS runner，并入 F23 框架自验收（C-139）。

**平台备注更新**（CHG-001 对 C-96）：macOS 上 **OpenCode 原生可用**（Windows 官方推荐 WSL）、**DeepSeek Harness 完整可用**（Windows 不支持其 Python SDK/PTY）——Mac 用户无需任何降级说明。


## 6. 平台适配矩阵速查

| 平台 | 档位 | 指令 | 技能 | 钩子 | 备注 |
|------|------|------|------|------|------|
| Codex | 主力 | 原生 AGENTS.md（链≤32KiB） | 原生 | 同款 schema | headless: `codex exec` |
| OpenCode | 主力 | 原生 | 原生 | 插件 ~50 行 | 无 Stop 门 → 靠 CI |
| Grok Build | 主力 | 原生（兼容面最宽） | 原生 | 直读 CC 配置；fail-open 须自兜 | headless: `grok -p` |
| DeepSeek Harness | 主力 | 原生 | 原生 | CC 桥 | rc 阶段；Windows 走 WSL（C-96 如实标注） |
| Claude Code | 主力 | **CLAUDE.md 桥** | **镜像** | 原生 | 唯一需要桥的平台 |
| Pi | 兼容 | 原生 | 原生 | 扩展 ~50 行 | 冒烟验证 |

## 7. ID 体系速查

| 前缀 | 对象 | 所在 | 前缀 | 对象 | 所在 |
|------|------|------|------|------|------|
| REQ- | 需求条目 | requirements/vN.md | CHG- | 变更单 | changes/ |
| RES- | 调研报告 | research/ | OSS- | 开源登记 | oss/ |
| DEC- | 决策 | decisions/ | LES- | 项目经验 | lessons/ |
| ISS- | 问题 | issues/ | KLES- | 跨项目经验 | ~/.keel/knowledge/ |
| APR- | 审批 | approvals/ | F-编号 | 功能（分支/尾注用） | features/ 目录名 |

全部全局递增永不复用，`gate new` 分配，CI 查重（通用规则）。

## 8. 实施路线图（建议，非确认项）

1. **W1 骨架**：目录树、AGENTS.md、模板（懒创建式）、config.json 注释模板；Trellis/Superpowers 迁移映射表初稿（F22）；**自举迁移**——把本设计轮的 features/decisions/research/DESIGN 迁入 keel 自身记录（F23，保留编号映射）。
2. **W2 gate 核心**：status / check（六门禁）/ new / index / trace / context-budget / sync / worktree / approve + 自身 pytest（Windows 优先）；.githooks + 尾注注入。
3. **W3 证据与 CI**：verify（重跑对账+树哈希）+ GitHub workflow + 分支保护/CODEOWNERS 配置指引 + Gitee/本地档退化配置指引。
4. **W4 技能**：按第 9 节清单写英文技能（含无歧义测试：让没读过的模型执行一遍）；镜像同步。
5. **W5 冒烟**：样例小项目全流程走通（含一次修 bug、一次变更、一次交接接力）；五主力平台技能触发实测与措辞校准；Pi 冒烟。
6. **W6 试点**：一个真实功能走全流程；校准初值（10KB / 28 天 / 封顶数）；按 C-105 做第一次门禁年检。

## 9. 技能载体建议（非确认项，实施轮细化）

用户入口（斜杠可调）：k-init（建骨架+配置三五问）/ k-migrate（F22 旧框架迁移+文档挖掘）/ k-new（需求→调研→方案→规划主流程调度）/ k-impl（认领功能进自主实施区）/ k-bugfix / k-change / k-review / k-accept / k-retro（复盘）/ k-handoff / k-status。模型自发原语：k-grill（访谈纪律）/ k-research / k-decide / k-evidence（证据与追溯）/ k-log（问题与摩擦记录）。总数 16 个，符合 C-121 封顶。

## 附录 A：功能 → 决策映射

F1→C-02~07｜F2→C-08~12｜F3→C-13~18｜F4→C-19~24｜F5→C-25~30+C-69｜F6→C-31~38｜F7→C-39~44｜F8→C-45~50｜F9→C-51~56｜F10→C-57~62｜F11→C-63~68｜F12→C-70~75｜F13→C-76~81｜F14→C-82~87｜F15→C-88~93｜F16→C-94~99｜F17→C-100~105｜F18→C-106~111｜F19→C-112~117｜F20→C-118~123｜F21→C-124~129｜F22→C-130~136｜F23→C-137~142｜F24→DEC-143~148｜运行时变更→DEC-149（取代 C-101）｜清单本体→C-01

## 附录 B：遗留校准项（试点后回填，均已在对应决策中标注）

- 常驻装载 ≤10KB、OSS 复查 28 天、知识库 ~100 条封顶、规则区条数封顶值——初值，W6 试点后校准；
- dsh 正式版发布后复评其主力档实测方式（C-96）；
- 各平台技能触发词敏感度——W5 实测校准（C-94）；
- 门禁清单年检节奏首次执行于 W6（C-105）。
