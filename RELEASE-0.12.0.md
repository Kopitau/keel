# keel 0.12.0 — Astra 指令一致性优化

CHG-017；依据 RES-909。目标是需求准确、功能有用、选型有据、实现清晰、功能验证、方便迭代和通俗说明。

## 修改

- 根指令统一授权持续性与澄清边界，技能不再要求重复确认、阶段性停工或越界执行历史前沿。
- 重要方案查当前且成熟的一手依据；小查询、可逆选择、小改动不强制形成整套记录。
- 复用同树有效证据；删除强制突变、medium 扩写、问题/经验配额与普通修复三次停工。
- 状态 next 只说明记录结构；普通规划/实现/修复等技能允许自然发现。技能探测检查 name/description 元数据，不再把重复技能名和固定英文句式当触发能力。
- README、设计说明、术语、模板与镜像同步；没有新依赖或门禁。
- 验证暴露的旧哈希问题 ISS-080：临时 Git 索引保留源时间戳，防止同长度快速源码变更被误判为证据仍新鲜；回归测试同时保护用户暂存区不被修改。

## 破坏点

这是流程语义调整，不改证据 JSON、记录编号或 gate 命令。若外部工具解析 next 的整句英文，需要改为使用已有结构化字段；next 不能推断用户意图。

元数据仍保留 k-init/k-migrate/k-accept 显式入口；其它 catalog 技能可隐式选择。旧的“11 个 User skills 全 false”、每阶段强制确认和固定措辞测试不再适用。

需求 v8、规划 v4、历史 DESIGN/DEC 保持原样；v9/v5 是实施工作版，不冒称用户已确认或验收。APR-009 只记录用户本轮优化范围授权。

## 消费项目要做的事

在已授权更新的项目执行 keel update；非交互环境可用 keel update --yes。检查有意义的本地补丁，保留项目自身的需求与权限边界。不要因框架更新重新审批已经授权的工作。

需要 Node ≥22.18.0。日常做相关测试；交付使用正式 verify 与完整 check，已有同树通过结果可以复用。新版本行为、指令文档检查与未实测的模型/远端效果应分开报告。
