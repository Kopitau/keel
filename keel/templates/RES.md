---
id: RES-000
title: short title
depth: standard
date: YYYY-MM-DD
features: []
oss: []
oss_none:
---

# RES-000 标题

档位：深度 / 标准 / 本地（自选并声明；统一规划确认时可要求升档）

> **开源表态（C-11，二选一，沉默不算表态）**：本次调研**选用**了某个开源项目 → 先 `gate new oss <name>` 建登记条目，再把编号填进 `oss: [OSS-00N]` 并删掉 `oss_none`；**一个都没选** → 删掉 `oss` 那行，在 `oss_none:` 后写清理由（「评估过但没采用」也要写，并写明什么条件下会重估）。
> `X-oss` 会拒绝两个字段都空的 RES——脚手架默认就是不合规状态。

## 调研问题

## 检索范围

## 候选对比

## 逐项证据

- 引用：
- 访问日期：
- 未核实项显式标注 `[未核实]`

## 结论

- 决定：
- 理由：
- 备选：

## 剩余不确定性
