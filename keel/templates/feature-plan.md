---
feature: F00
slug: f00-name
plan_version: v1
replaces: null
change: null
wave: W1
req: [REQ-000]
blocked_by: []
---

# F00 标题 — 规划 v1

确认后保留历史；授权内的语义迭代用 CHG 与新版本，复用已有授权。不把实施授权当作用户已确认本文。小改动可只用范围与测试说明。

## 范围

写选定方案和本次交付义务；候选、淘汰机制及其研究实验留在 RES，不自动加入实施。当前开工/运行状态只在 handoff 维护。

## 非范围

## 测试义务

- 分级（C-31）：核心 / 辅助 / 豁免
- 验收按 verification 类型：auto 用公共接口行为测试，machine-doc 用相关协议/文档检查，manual 写真实环境证据条件；自动检查名带 REQ-nnn/AC-i 便于映射，不冒充模型实测
- 联动（C-38）：接口耦合表中本功能相关条目，测试名带 `I-nn`
- 公共接口：选择能真实观察承诺行为的稳定接口，覆盖必要联动与失败路径；接口尚不存在时声明 proxy 与解除条件，不限制必要测试条数

| REQ/AC | 验证类型与公共接口/真实环境 | 必要证据或 proxy 解除条件 |
|---|---|---|
| REQ-000/AC-1 | | |

## 内部步骤

每步交付可观察的结果；只列有用的步骤。继续当前授权范围内的下一步，不因阶段切换重问，也不自动扩到其它历史功能。

1. （verify: `一条命令`）

## 预计触碰文件

## 依赖

`blocked_by:` 列真实依赖。gate 据 summary/claim 推导前沿，这是记录状态，不是任务授权或依赖实际可用的证明。
