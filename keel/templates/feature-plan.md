---
feature: F00
slug: f00-name
plan_version: v1
replaces: null
change: null
wave: W1
req: [REQ-000]
blocked_by: []
---

# F00 标题 — 规划 v1

确认后保留历史；授权内的语义迭代用 CHG 与新版本，复用已有授权。不把实施授权当作用户已确认本文。小改动可只用范围与测试说明。

## 范围

## 非范围

## 测试义务

- 分级（C-31）：核心 / 辅助 / 豁免
- 验收按 verification 类型：auto 用公共接口行为测试，machine-doc 用相关协议/文档检查，manual 写真实环境证据条件；自动检查名带 REQ-nnn/AC-i 便于映射，不冒充模型实测
- 联动（C-38）：接口耦合表中本功能相关条目，测试名带 `I-nn`
- 缝（RES-904 §6）：黑盒测试挂在哪一层——尽量最高、尽量只一条；缝尚不存在的 AC 在此声明 proxy 与解除条件

| REQ/AC | 缝（黑盒测试挂在哪） | 状态（真验收 / proxy 至 Fnn） |
|---|---|---|
| REQ-000/AC-1 | | |

## 内部步骤

每步交付可观察的结果；只列有用的步骤。继续当前授权范围内的下一步，不因阶段切换重问，也不自动扩到其它历史功能。

1. （verify: `一条命令`）

## 预计触碰文件

## 依赖

`blocked_by:` 列真实依赖。gate 据 summary/claim 推导前沿，这是记录状态，不是任务授权或依赖实际可用的证明。
