---
feature: F4
slug: f04-unified-plan
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-004]
blocked_by: []
---

# F4 统一实施规划 — 规划 v2

确认即冻结。本版把 requirements v4 展开为 overview v3 与 24 份 owner 计划新版，旧计划不改。

## 本版范围

- 每份最新功能计划都有唯一 `feature:`、完整 `req:`、显式 `blocked_by:`、verification 与黑盒测试缝。
- `blocked_by` 只写“开工前必须已有 summary.md”的硬阻塞；普通接口耦合留在 overview I-nn，避免把部分实现仓误写成瀑布。
- `gate status` 的 frontier/blocked/proxy 与 G-plan 非法编号、自指、悬空、环判据保持可执行。

## 非范围

本规划切片不实现各 owner 的行为；不修改 overview v1/v2 或旧 feature plan。

## 测试义务

| REQ/AC | verification | 缝 / 证据 |
|---|---|---|
| REQ-004/AC-1 | auto | `gate index` 只保留一个 current，历史版本仍存在 |
| REQ-004/AC-2..4 | machine-doc | overview/feature plan 必填结构与耦合表解析 |
| REQ-004/AC-5 | manual | 用户批准的 CHG-010/APR-003 可追到本规划范围 |
| REQ-004/AC-6 | machine-doc | worklog 追加式且能定位每个切片证据 |
| REQ-004/AC-7..9 | auto | `req:`、`blocked_by:`、frontier/blocked/proxy 黑盒 |

## 内部步骤

1. 新建 overview v3 与 24 份计划新版并切 INDEX（verify: `node tools/gate/gate.ts check --quick`）。
2. 补最新计划元数据与 frontier 负向测试（verify: `node --test tests/dec169-frontier.test.ts tests/w2-gate.test.ts`）。
3. 逐 owner 对账 plan REQ/AC 数与 v4（verify: `node tools/gate/gate.ts trace`）。

## 预计触碰文件

`keel/plan/overview-v3.md`、`keel/plan/INDEX.md`、`keel/features/*/plan/v*.md`、F4/F11 worklog、规划结构测试。

## 依赖

I-04、I-05、I-19、I-25。
