---
feature: F16
slug: f16-platforms
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-016]
blocked_by: []
aligns: [DEC-170]
---

# F16 六平台适配 — 规划 v2

确认即冻结。本版补 Codex metadata 精确集合、镜像 stale 与逐 harness headless/触发证据。

## 本版范围

- User skills 恰为 11 个并 `allow_implicit_invocation: false`；Model skills 恰为 5 个并为 true；两组不交叠且并集等于 catalog。
- `gate sync` 先生成/校验所有 `agents/openai.yaml`，再复制 `.claude` 镜像；missing/stale/分类漂移使 X-skills FAIL。
- headless 页逐家记录命令、输入、成功判据、来源；六家真实触发证据按 v4 条件留存。

## 非范围

不复制流程逻辑到平台私有文件；不因某 harness 不可用而静默降级。

## 测试义务

REQ-016/AC-1..10 一一对应 `[auto, auto, auto, machine-doc, auto, auto, machine-doc, manual, auto, auto]`。自动缝为 `gate sync`、X-skills 与布局检查；AC-8 是逐家人工证据，Codex 还要实证 User/Model implicit 各一例。

## 内部步骤

1. metadata 分类/生成/stale 负向矩阵（verify: `node --test tests/dec170-openai-yaml.test.ts`）。
2. headless 配方结构与失败不降级（verify: `node --test tests/chg010-headless.test.ts`）。
3. Codex 实测并逐步补五主力+Pi 证据（verify: 证据记录绑定 tree hash）。

## 预计触碰文件

`tools/gate/skills.ts`、`sync.ts`、`.agents/skills/*/agents/openai.yaml`、`.claude/skills/`、headless 配方页、平台证据记录、相关测试。

## 依赖

I-11、I-18、I-21。
