---
feature: F16
slug: f16-platforms
plan_version: v3
replaces: v2.md
change: CHG-012
wave: R5-0.9.1
req: [REQ-016]
blocked_by: []
aligns: [DEC-170, RES-908]
---

# F16 六平台适配 — 规划 v3（规划补充：Cursor 客户端）

确认即冻结（语义）。v2 的内容不变，本版只追加 Cursor 客户端作为兼容档（CHG-012 / C-23）。

## 范围

- Cursor 桌面客户端按现有契约接入：根 `AGENTS.md` + `.agents/skills/k-*/SKILL.md`，技能与根指令零改动（RES-908）。
- 登记：`platforms.compatible` 加 `cursor`；`triggers.ts` 探测表与发现表；`platform-limits.md` 一行；`headless.md` 的 `## cursor`（客户端为准，无 headless 配方，CLI `agent` 同名冲突、未实测）。

## 非范围

- Cursor CLI（`agent`）的 headless 复审配方与安装；`.cursor/rules` / `.cursor/skills` 的第二份布局（C-94：不复制流程逻辑到平台专属文件）。
- 给 User skills 加 `disable-model-invocation`（方案 B，被否，见 RES-908）。

## 测试义务

| REQ/AC | 缝 | 状态 |
|---|---|---|
| REQ-016/AC-1 | `HARNESS_DISCOVERY` / config / 技能前言的协议检查（machine-doc） | 真验收：`tests/chg012-cursor.test.ts` |
| REQ-016/AC-8 | 真实 Cursor 客户端触发冒烟 | 人工证据（proxy 至用户在装有 Cursor 的机器上跑一次并记 worklog） |
| 平台计数 | `tests/w5-smoke.test.ts` 6 → 7 | 真验收 |

## 内部步骤

1. 配置 + 探测表 + 发现表 + 文档（verify: `node --test tests/chg012-cursor.test.ts tests/w5-smoke.test.ts`）
2. 需求 v6 + CHG-012 + APR-006 草稿；用户点头后 approve、切 INDEX（verify: `gate check --quick` 绿）
3. 0.9.1 + `RELEASE-0.9.1.md`（verify: `tests/chg011-release.test.ts`）

## 预计触碰文件

`keel/config.json`、`keel/templates/config.json`、`tools/cli/init.js`、`tools/gate/triggers.ts`、`tools/gate/platform-limits.md`、`keel/review/headless.md`、`tests/chg012-cursor.test.ts`、`tests/w5-smoke.test.ts`、`keel/requirements/v6.md`、`keel/changes/CHG-012-*.md`、`keel/approvals/APR-006-*.md`、`RELEASE-0.9.1.md`、`package.json`、`package-lock.json`。

## 依赖

无（`blocked_by: []`）。
