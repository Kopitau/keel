---
feature: F5
slug: f05-overhead
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-005]
blocked_by: []
---

# F5 框架开销控制 — 规划 v2

确认即冻结。本版对账“机械环节零 token、记录不自动灌上下文、降耗不损智能”的验证边界。

## 测试义务

REQ-005/AC-1..5 一一对应 `[auto, auto, machine-doc, manual, manual]`。自动缝为 gate 的 status/index/hash/sync；machine-doc 检查导航分层；两条 manual 分别记录长文记录员使用条件与“没有因省 token 降低功能/模型”的人工核对。

## 内部步骤

1. 机械命令与无模型依赖契约（verify: `node --test tests/chg010-overhead.test.ts`）。
2. 常驻装载/导航与人工质量证据（verify: `gate check --quick` + worklog 证据）。

## 预计触碰文件

`AGENTS.md`、autoload/budget 检查、相关技能措辞、F5 worklog 与测试。

## 依赖

I-12、I-15。
