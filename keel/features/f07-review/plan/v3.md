---
feature: F7
slug: f07-review
plan_version: v3
replaces: v2.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-007, REQ-027, REQ-028]
blocked_by: []
aligns: [DEC-159, DEC-160, DEC-177, DEC-178, DEC-182]
---

# F7 独立评审与人工验收 — 规划 v3

确认即冻结。v2 的自动回路方向保留；本版补齐 headless、攻击探针、追加式历史和用户边界。

## 本版范围

- 仓库内逐 harness headless 配方必须写命令、输入、成功判据与来源；跨 harness 失败不得静默同源。
- blocking finding 的 repro 是攻击探针：问题存在退出 0；修复或拒绝后非 0。ingest 先验真，clear 复跑同命令并追加结果。
- 三轮熔断按同指纹；攻击面清单有固定位置、最小结构和追加责任；评审只报不改，APR/merge 仍由用户边界控制。

## 非范围

不自动批准 CHG/APR、不自动 merge；不把无复现命令的 finding 提升成 blocking ISS。

## 测试义务

| REQ | verification 数组 | 主要缝 |
|---|---|---|
| REQ-007/AC-1..5 | `[machine-doc ×5]` | pack 五样、隔离记录、验收包结构 |
| REQ-027/AC-1..9 | `[auto, auto, manual, auto, auto, auto, machine-doc, manual, auto]` | `gate loop pack/ingest/clear/status`；AC-3/8 留人工证据 |
| REQ-028/AC-1..5 | `[auto, auto, machine-doc, machine-doc, machine-doc]` | 路径分类器与三份 living checklist |

## 内部步骤

1. headless 配方页与 pack 输入裁剪（verify: 配方结构检查 + `gate loop pack` fixture）。
2. ingest 攻击探针先验与拒绝路径（verify: `node --test tests/chg010-review-loop.test.ts`）。
3. clear 追加 `repro_runs`、重复 clear 不抹历史并关闭 ISS-036（verify: 同一攻击探针 0→非0→重复 clear）。
4. 异构 fresh review 通过后才声称完成（verify: `node tools/gate/gate.ts loop status`）。

## 预计触碰文件

`tools/gate/reviewloop.ts`、`harness.ts`、`.agents/skills/k-review/SKILL.md`、`keel/review/*.md`、headless 配方页、`tests/chg008-review.test.ts`、`tests/chg010-review-loop.test.ts`。

## 依赖

I-06、I-07、I-22。与 F10/F6 共享 reviewloop/evidence 时由 F7 切片单写。
