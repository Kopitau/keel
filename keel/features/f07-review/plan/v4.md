---
feature: F7
slug: f07-review
plan_version: v4
replaces: v3.md
change: CHG-013
wave: R5-0.9.1
req: [REQ-007, REQ-027, REQ-028]
blocked_by: []
aligns: [DEC-184]
---

# F7 独立评审 — 规划 v4（规划补充：去掉攻击面视角、不强制异构）

确认即冻结（语义）。v3 的方案级回路不变；本版按 DEC-184 去掉视角分类与异构规则（CHG-013 / C-23）。

## 范围

- 评审者 = 未参与实现的空白上下文子代理（同一 harness 即可）；两轴（spec / 工程）+ 按 C-31 分级的鲁棒性 / 需求清单。
- `reviewloop.ts` 删除 lens / 异构 / `paths` / `append-attack`；evidence.review 去掉相关字段；attack-surface.md 删除；k-review 重写；headless.md 改可选；config 去掉 `heterogeneous_review`。

## 非范围

- 回路的探针协议（DEC-182）、处置表出处（ISS-056）、范围绑定（ISS-055）不变。
- 不删除 headless 配方（跨 harness 仍可选）。

## 测试义务

| REQ/AC | 缝 | 状态 |
|---|---|---|
| REQ-027/AC-3 | `gate loop ingest` 接受同源空白上下文 reviewer 并记录 | 真验收：`tests/chg008-review.test.ts` |
| REQ-028/AC-1..3 | k-review 与两份清单的协议检查（machine-doc） | 真验收：`tests/chg008-review.test.ts` |
| 回归 | 旧 lens / 异构守卫删除，其余回路用例不变 | `node --test` 全绿 |

## 内部步骤

1. 代码与测试（verify: `npx tsc --noEmit` + `node --test`）
2. 记录：DEC-184、CHG-013、v6 三处 REQ、APR-006 工件、ISS/DEC 备注（verify: `gate index` + `gate check --quick`）

## 预计触碰文件

`tools/gate/reviewloop.ts`、`tools/gate/evidence.ts`、`tools/gate/gate.ts`、`tools/cli/init.js`、`keel/config.json`、`keel/templates/config.json`、`.agents/skills/k-review/SKILL.md`、`keel/review/{headless,robustness}.md`、`tests/chg008-review.test.ts`、`tests/r4-rework.test.ts`、`tests/chg010-headless.test.ts`、`keel/requirements/v6.md`、`keel/changes/CHG-013-*.md`、`keel/decisions/DEC-184-*.md`。

## 依赖

无。
