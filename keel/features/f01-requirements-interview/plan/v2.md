---
feature: F1
slug: f01-requirements-interview
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-001]
blocked_by: []
---

# F1 需求访谈与记录 — 规划 v2

确认即冻结。本版不改访谈行为，只把 v4 的 gap-hunt、整份点头与同轮原话证据变成可运行的记录协议检查。

## 本版范围

- 需求条目字段、GWT、NEEDS-CLARIFICATION、独立 gap-hunt 和基线确认链均可由机器读取。
- 可查事实仍由 agent 自查；用户只回答取舍，不能用模板替代真实决定。

## 测试义务

REQ-001/AC-1..6 的 verification 均为 `machine-doc`。缝为 requirements parser、gap-hunt 结构与 DEC/APR 原话指针；结构检查必须能失败，但不冒充用户实际作答。

## 内部步骤

1. 对账模板与 `k-grill` 输出协议（verify: `node --test tests/chg010-record-protocols.test.ts`）。
2. gap-hunt/整份确认链结构检查（verify: `node tools/gate/gate.ts check --quick`）。

## 预计触碰文件

`.agents/skills/k-grill/SKILL.md`、requirements/gap-hunt 模板、记录协议测试、F1 worklog。

## 依赖

I-01。
