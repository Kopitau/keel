---
feature: F21
slug: f21-config
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-021]
blocked_by: []
---

# F21 语言与项目类型配置 — 规划 v2

确认即冻结。本版把 0.8.0 版本、多画像选择和字段/正文语言约束纳入对账。

## 本版范围

config 更新到 0.8.0 时仍集中保存画像、身份、平台、预算与可选项；`_comment` 可存在且 gate 忽略。多画像按触碰范围选择，unset 明确失败或降级，不能静默绿。

## 测试义务

REQ-021/AC-1..5 一一对应 `[machine-doc, machine-doc, auto, machine-doc, auto]`。自动缝为 config parser/profile selector；machine-doc 检查英文机器字段、中文记录正文、CONTEXT 术语和 notebook 边界。

## 内部步骤

1. config 0.8.0/schema 与 `_comment` 兼容（verify: `node --test tests/chg010-config.test.ts`）。
2. 多画像/触碰范围/unset 正反向（verify: 同测试 + `gate verify` fixture）。

## 预计触碰文件

`keel/config.json`、config 模板/parser、profile selector、CONTEXT、F21 worklog 与测试。版本号实际写入与 installer 清单由 F23 串行。

## 依赖

I-15、I-17、I-21。
