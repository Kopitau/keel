---
feature: F12
slug: f12-handoff
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-012]
blocked_by: []
---

# F12 会话交接与断点续传 — 规划 v2

确认即冻结。本版对账三跳、OVERVIEW、人工恢复证据和平台无关边界。

## 测试义务

REQ-012/AC-1..4 一一对应 `[machine-doc, auto, manual, machine-doc]`。自动缝为 `gate status` 路径/计数；AC-3 由另一 harness 只读 handoff+plan+worklog 后复述下一步的人工证据；不解析平台会话 JSON。

## 内部步骤

1. status/交接固定节与导航结构（verify: `node --test tests/chg010-handoff.test.ts`）。
2. fresh context 恢复演练并记录误差（verify: 人工证据绑定 tree hash）。

## 预计触碰文件

`.agents/skills/k-handoff/SKILL.md`、handoff/journal 模板、status 测试、F12 worklog。已有用户 handoff 改动必须原样保留。

## 依赖

I-25。
