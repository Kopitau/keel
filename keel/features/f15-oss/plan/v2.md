---
feature: F15
slug: f15-oss
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-015]
blocked_by: []
---

# F15 开源复用登记与追踪 — 规划 v2

确认即冻结。本版对账直接依赖、RES 选用、许可证和周期复查的验证边界。

## 测试义务

REQ-015/AC-1..4 一一对应 `[auto, machine-doc, manual, manual]`。自动缝为 package direct deps↔OSS；machine-doc 检查 RES 选用声明；许可证升级和 28 天复查结论由人工证据承担，不自动改代码。

## 内部步骤

1. 直接依赖/OSS 与 RES stance 对账（verify: `node --test tests/chg010-oss.test.ts`）。
2. 到期/许可证样本人工复查（verify: OSS worklog 记录日期、版本与结论）。

## 预计触碰文件

OSS/RES 检查、OSS 模板、`k-log`/`k-research`、F15 worklog 与测试。

## 依赖

I-03。
