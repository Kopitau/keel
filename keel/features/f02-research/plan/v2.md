---
feature: F2
slug: f02-research
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-002]
blocked_by: []
---

# F2 强制调研 — 规划 v2

确认即冻结。本版只增加 requirements v4/CHG-010 的验证与 legacy RES 迁移义务；v1 未变范围继续有效。

## 本版范围

- G-research 消费 `<records_dir>/migrations/res-citation-legacy.json`，按版本 cutoff、ID、相对路径和 DEC-144 规范化全文 SHA-256 判定 URL 子检查。
- 正文已有合格 URL 为 PASS；升级前零 URL 且唯一 manifest 条目完全匹配为 WARN；其余为 FAIL。总 G-research 仍取所有子检查最严重等级。
- manifest 只豁免 URL 子检查，不豁免 RES 结构、候选、重大 DEC、OSS 或离线证据。

## 非范围

F2 不生成 manifest、不改旧 RES；生成端属于 F23/F22（I-20）。

## 测试义务

| REQ/AC | verification | 缝 / 证据 |
|---|---|---|
| REQ-002/AC-1..2 | machine-doc | RES 结构读取端与 `gate check` 诊断 |
| REQ-002/AC-3 | auto | fixture 中重大 DEC 缺 RES 时 G-research FAIL |
| REQ-002/AC-4 | machine-doc | RES 的 OSS 声明可机读且能追到 OSS 记录 |
| REQ-002/AC-5 | manual | 离线资料包记录来源、版本、日期、路径或停止原因 |
| REQ-002/AC-6 | auto | `<0.8.0→>=0.8.0` 的 PASS/WARN/FAIL 黑盒矩阵 |

## 内部步骤

1. manifest schema/hash/cutoff 纯函数与负向 fixtures（verify: `node --test tests/chg010-legacy-res.test.ts`）。
2. 接入 G-research，证明 legacy 只影响 URL 子检查（verify: `node tools/gate/gate.ts check --quick`）。
3. 在消费项目跑升级前后样本并记录 WARN/FAIL（verify: 消费项目 `node tools/gate/gate.ts check`）。

## 预计触碰文件

`tools/gate/rescheck.ts`、`tools/gate/check.ts`、`tests/chg010-legacy-res.test.ts`、F2 worklog；manifest 文件由 F23 写。

## 依赖

I-02、I-03、I-20。与 F23 修改同一 manifest 契约时接口先行、文件串行。
