---
feature: F18
slug: f18-approvals
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-018]
blocked_by: []
---

# F18 审批记录机制 — 规划 v2

确认即冻结。本版补审批目录保护的 GitHub/Gitee/local 三档判据并联动 CHG/APR 关系。

## 本版范围

- approved APR 继续要求规范化内容 hash、真人 identity；agent 环境代提交必须先记录用户原话委托。
- GitHub/Gitee 档要求 approvals 路径由真实 owner 保护；local 档明确只剩文档级保护，不冒充平台审批。
- 为 I-23 提供 approved APR artifact 查询，不放宽身份规则。

## 非范围

不强制签名、不防真人恶意、不自动批准或合并。

## 测试义务

REQ-018/AC-1..5 一一对应 `[machine-doc, auto, machine-doc, machine-doc, auto]`。自动缝为 `gate approve`、pre-commit APR guard、X-apr、X-owners；覆盖 agent identity、无 delegation、placeholder owner、local downgrade。

## 内部步骤

1. APR artifact/identity 读取端契约（verify: `node --test tests/chg010-approvals.test.ts`）。
2. CODEOWNERS 三档正反向 fixtures（verify: 同测试 + `gate check --quick`）。
3. 与 F11 的 CHG→APR 关系联动（verify: `tests/chg010-change-chain.test.ts`）。

## 预计触碰文件

`tools/gate/approve.ts`、`hook.ts`、`check.ts`、`.github/CODEOWNERS`、审批文档/测试、F18 worklog。

## 依赖

I-07、I-23。
