---
feature: F17
slug: f17-gate
plan_version: v3
replaces: v2.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-017, REQ-026]
blocked_by: []
aligns: [DEC-175]
---

# F17 门禁强制层 — 规划 v3

确认即冻结。Node+TS 单一 gate 不变；本版补 v4 的 workflow/真实运行拆分、CHG 关系和空项目语义对账。

## 本版范围

- REQ-017/AC-3 只验 workflow 调同一 gate 且声明 Windows/macOS/Linux × Node 22/24；AC-4 只认真实 run 证据，当前保持一个明确 proxy。
- G-req 接入 I-23 的 CHG/APR 关系；G-plan 接入 verification 与 plan metadata；诊断保持 PASS/WARN/FAIL + fix。
- REQ-026 四种空项目/活动/未决语义继续黑盒覆盖。

## 非范围

本地 workflow 文件存在不等于 CI 成功；不为平台复制第二份 gate。

## 测试义务

| REQ | verification | 缝 |
|---|---|---|
| REQ-017/AC-1..5 | `[auto, auto, auto, manual, auto]` | 启动器、`gate check`、workflow 契约；AC-4 为真实 GitHub 证据 |
| REQ-026/AC-1..4 | `[auto, auto, auto, auto]` | 真空/有活动/耦合缺失/活 NEEDS-CLARIFICATION fixtures |

## 内部步骤

1. v4 gate 判据与诊断契约（verify: `node --test tests/chg010-gates.test.ts`）。
2. workflow 六格静态契约；保留 AC-4 proxy（verify: `node --test tests/w6-pilot.test.ts`）。
3. GitHub 首跑后记录 run URL/ID、SHA、六 job、日期与工件 hash，再移除 proxy（verify: `node tools/gate/gate.ts trace`）。

## 预计触碰文件

`tools/gate/check.ts`、`trace.ts`、`.github/workflows/gate.yml`、`tests/chg010-gates.test.ts`、`tests/w6-pilot.test.ts`、CI 证据记录。

## 依赖

I-12、I-16、I-17、I-23、I-24。
