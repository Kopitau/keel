---
feature: F17
slug: f17-gate
plan_version: v2
replaces: v1.md
change: CHG-001
wave: W1-stub/W2
req: REQ-017
---

# F17 门禁强制层 — 规划 v2

## 范围

gate 运行时 = Node + TypeScript，直接跑 `.ts`（DEC-149/151）。W1：`status` + `hash` 桩、版本门、启动器。W2：六门禁真身。

## 非范围

W1 不实现 check/new/index/trace/verify/approve。不引入除 `typescript` 外的 devDependency。

## 测试义务

- 核心：`node --test`（DEC-152）；`tsc --noEmit`（DEC-154）
- W1：status 打印 handoff；hash 规范化
- 分级：gate 代码 = 核心

## 内部步骤

1. 回滚 `gate.py` / pytest
2. `gate.ts` + 模块拆分（hash/config/paths/version）
3. W2 在同一入口加子命令

## 预计触碰文件

`tools/gate/gate.ts` `tools/gate/*.ts` `tools/gate/gate.sh` `tools/gate/gate.ps1` `package.json` `tsconfig.json`

## 依赖

F21 config、F24 启动器与哈希、F23 上一版管下一版（W3）
