---
feature: F11
slug: f11-change
plan_version: v2
replaces: v1.md
change: CHG-010
wave: R5-0.8.0
req: [REQ-011]
blocked_by: []
---

# F11 需求变更管理 — 规划 v2

确认即冻结。本版执行 v4 基线联动，并补 confirmed REQ→approved CHG→approved APR 的机器关系。

## 本版范围

- 当前 requirements 仍硬冻结；变更只经 approved CHG 产生新版本并重索引。
- CHG 生效必须联动 DEC 复核、所有 owner 计划新版、测试义务与 evidence 失效。
- confirmed REQ 声称来源 CHG 时，该 CHG 必须 approved，且 approved APR 的 artifact hash 必须能追到它；proposed CHG 不得产生当前 confirmed REQ。

## 非范围

不修改 frozen v1～v3；纯文字澄清仍按 AC 语义是否变化判定。

## 测试义务

REQ-011/AC-1..5 一一对应 `[auto, auto, auto, machine-doc, auto]`。黑盒缝为 `gate check`/G-req、`gate index` 与 tree hash；必须含 proposed CHG、approved 但无 APR、APR hash 不匹配、完整链通过四组 fixtures。

## 内部步骤

1. 本 overview v3 + 24 份计划新版 + INDEX（verify: `node tools/gate/gate.ts check --quick`）。
2. CHG/APR artifact 关系读取端与 G-req 负向测试（verify: `node --test tests/chg010-change-chain.test.ts`）。
3. 修改当前版与旧 evidence stale 联动（verify: `node tools/gate/gate.ts verify` 后变更 fixture 再 `check`）。

## 预计触碰文件

`tools/gate/check.ts`、CHG/APR 解析模块、`keel/plan/`、`keel/features/*/plan/`、`tests/chg010-change-chain.test.ts`、F11 worklog。

## 依赖

I-01、I-19、I-23。
