---
feature: F24
slug: f24-cross-platform
plan_version: v1
replaces: null
change: CHG-001
wave: W1
req: REQ-024
---

# F24 跨平台支持 — 规划 v1

## 范围

Windows / macOS 开发机 + Linux CI 一等公民（DEC-143）。W1 交付：规范化哈希函数与测试、`.gitattributes eol=lf`、`gate.sh`/`gate.ps1`、Node≥22.18.0 拒绝、路径只用 runtime API。

## 非范围

macOS/Linux CI 矩阵与 DEC-148 fixture 在 W3/W5。不为每平台复制 gate 逻辑。

## 测试义务

- 核心：CRLF 与 LF、BOM 的哈希相同（W1 即测）
- 核心：低于 22.18.0 的版本比较为 false
- 辅助：启动器文件存在且含版本门
- 联动：I-16

## 内部步骤

1. 实现 `tools/gate/hash.ts` `node-version.ts` `paths.ts`
2. 双启动器转调 `gate.ts`
3. hooks 调 `gate.sh`（Windows 走 Git Bash）
4. W3 把同一测试接到 macOS runner

## 预计触碰文件

- `tools/gate/hash.ts` `node-version.ts` `paths.ts` `gate.sh` `gate.ps1`
- `.gitattributes` `.githooks/`
- `tests/w1-skeleton.test.ts`

## 依赖

F17（gate 入口）、F6/F18（哈希消费者，W2/W3 接线）
