{
  "_comment": "keel/config.json (C-129). Keys starting with _ are comments; gate (W2) must ignore them.",
  "schema_version": 1,
  "project_name": "keel",
  "records_dir": "keel",
  "enforcement_tier": "local",
  "wave": "P2-rework",
  "_comment_tier": "github | gitee | local. Remotes not configured in W1; local = hooks + gate-before-merge + hash APR. Honest label: deters mistakes, not malice (C-48).",
  "profiles": {
    "_comment": "Test profiles (C-126/C-128). Gate verify (W3) selects by touched paths.",
    "active": ["keel-gate"],
    "keel-gate": {
      "language": "typescript",
      "runtime": "node",
      "node_min": "22.18.0",
      "test_command": "node --test",
      "typecheck": "tsc --noEmit",
      "core_marker": "core"
    },
    "python-cli": {
      "language": "python",
      "test_command": "python -m pytest -q",
      "core_marker": "core",
      "note": "Consumer-project profile (DEC-126). Not used for keel's own gate tests (DEC-152)."
    },
    "ds-ml": {
      "language": "python",
      "test_command": "python -m pytest -q",
      "notebook": "nbmake/nbval (not wired in W1)",
      "metrics_json_in_evidence": true
    },
    "ts-js": {
      "language": "typescript",
      "test_command": "npx vitest run",
      "e2e": "npx playwright test"
    },
    "other": {
      "language": "other",
      "test_command": "echo 'configure junit-compatible command'",
      "junit_compatible": true
    }
  },
  "identities": {
    "_comment": "Humans submit APR (C-107). Agent names must not appear as APR commit authors. Fill humans after git identity is known.",
    "humans": [],
    "agents": [
      {
        "name": "keel-agent",
        "email": "agent@keel.local",
        "note": "W1 local git identity for non-APR commits. Not allowed to land APR."
      }
    ]
  },
  "platforms": {
    "primary": ["claude-code", "codex", "opencode", "grok-build", "deepseek-harness"],
    "compatible": ["pi"],
    "os_matrix": ["windows", "macos", "linux"],
    "deepseek_harness_windows": "WSL required for its Python SDK/PTY (C-96). Files are readable on Windows. macOS: dsh fully available (CHG-001)."
  },
  "budget": {
    "_comment": "C-118 values. W6 2026-08-21 measured autoload 6427/10240 and KEPT all numbers. Soft over = warn; platform hard over = fail (C-119).",
    "agents_md_max_lines": 150,
    "agents_md_chain_max_bytes": 32768,
    "skill_max_lines": 500,
    "skill_description_max_chars": 1024,
    "autoload_max_bytes": 10240,
    "skill_count_cap": 16
  },
  "optional": {
    "no_mistakes": false,
    "heterogeneous_review": false,
    "recorder_medium_for_longform": true
  },
  "oss_review_days": 28,
  "knowledge_cap": 100,
  "rules_area_cap": null,
  "_comment_caps": "W6 2026-08-21 KEEP: oss_review_days=28, knowledge_cap=100, rules_area_cap=null (no rules dir). See keel/features/f20-context-budget/calibration-w6.md."
}
