# 2026-08-28-03 — P1 verification 数组协议

## 做了什么 / 为什么

P1 的 F6 第一个切片实现 DEC-174 requirements 协议：每条 REQ 的 acceptance 与 verification 必须等长，类型只能是 `auto`、`machine-doc`、`manual`。G-req 与 trace 现在共用同一 parser，不再分别用正则猜 AC 数。

## 当前功能与阶段

F6，P1 verification parser 切片完成；P1 尚有 trace 展示/claimed scope/proxy 语义，以及 evidence stale 联动切片。

## 红绿证据

- 红：四场景为 1 pass / 3 fail，旧 G-req 错误放行少项、多项和非法类型。
- 绿：四场景 4/4；相关兼容与 trace 回归 27/27；TypeScript 干净。
- quick 为 PASS_WITH_WARN；真实 v4 的 28 个 verification 数组全部有效。
- 最终完整命令、退出码与 tree hash 见 `keel/evidence/verify.json`。

## 边界与下一步

- 完全没有声明 verification 协议的旧格式仍可读；一旦文件出现“验证方式”或任一 verification 字段，就对全部 REQ 严格校验。
- 下一会话做 F6 plan v2 步骤 2：trace 输出每条 AC 的 verification 类型与 claimed scope，证明 proxy 只 WARN、不冒充 PASS。
- P6 仍只做本地，不触发真实 GitHub 六格；ISS-036 留 P2；用户在途 `keel/handoff.md` 不纳入提交。
