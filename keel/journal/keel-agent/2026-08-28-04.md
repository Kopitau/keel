# 2026-08-28-04 — P1 trace 验证方式与验收范围

## 做了什么 / 为什么

`gate trace` 现在把每条 AC 的 `auto` / `machine-doc` / `manual` 与 REQ 是否因 owner `summary.md` 进入 claimed scope 放在同一行。此前全库缺口可见，但读者无法区分“尚未开工的报告缺口”和“已声明完成、会被 X-trace 强制”的范围。

## 红绿证据

- 红：`tests/chg010-verification.test.ts` 4 pass / 2 fail，缺失 scope/verification 两列。
- 绿：定向 trace 回归 37/37；TypeScript 通过；基线 223 个测试名。
- 真实矩阵：claimed 2/28，claimed uncovered 0；REQ-017/AC-4 显示 `manual` 且仍是 proxy。

## 连续执行边界

用户要求“继续”后持续推进，因此本切片完成后不结束，直接进入 F6 plan v2 步骤 3 的 evidence stale/review 追加历史联动。P6 真实 GitHub 六格仍不执行。
