# 2026-08-28-01 — requirements v4 P0 规划基线

## 做了什么 / 为什么

APR-003 已批准 requirements v4，但当前统一规划仍是 overview v2，违反 `k-impl` 的开工前置。按 CHG-010 新建 overview v3，并为 24 个 owner 功能全部新建计划版本；F7/F17 为 v3，其余为 v2。

## 当前功能与阶段

F4/F11；CHG-010 P0 规划完成，P1 行为实现未开始。

## 关键选择

- overview 完整保留 I-01～I-18，新增 I-19～I-25；计划按 P0～P6 串行相交文件。
- `blocked_by` 只表示必须等完整 feature summary 的硬阻塞；普通耦合留 I-nn，不把既有部分实现仓误写成瀑布。
- overview v3 首次 verify 暴露 plan current 硬编码；ISS-051 以版本无关不变量闭环，未改测试名。

## 证据与遗留

- 24 latest plans 的 owner/metadata/verify 段审计 PASS；I-01～I-25 唯一。
- quick gate PASS_WITH_WARN；唯一 WARN 为 REQ-017/AC-4 真实 GitHub 六格证据 proxy。
- 最终完整命令、退出码与 tree hash 见 `keel/evidence/verify.json`。
- ISS-036 等 P2；F4 worklog 的 defense-failed 经验候选等 retro；用户原 handoff 改动不纳入本切片提交。

## 下一步

从 `overview-v3.md` P1 和 F11 plan v2 开始：先审计现有 CHG→REQ→APR/hash 判据，再以黑盒红灯补真正缺口。一个会话只做一个切片。
