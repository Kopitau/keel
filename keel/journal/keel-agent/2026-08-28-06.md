# 2026-08-28-06 — ingest 先验攻击探针

## 缺口

旧 `gate loop ingest` 把“有命令字符串”误当“问题可复现”，没有执行命令。一个已经返回非 0、表示攻击已被拒绝的 finding 仍会开 blocking ISS。

## 实现与证据

- 唯一 ISS 写入端先跑探针；退出 0 才开单。
- 非 0 与缺命令都不创建 ISS，保留待核实观测。
- 正向 ISS 保存首次 exit/time/tree hash，后续 clear 有可追入口。
- 新三路径加入既有端到端文件：修复前 3/5，修复后 5/5。

## 边界

测试使用仓内 probe 文件，不依赖 cmd/sh 内联引号。P6 GitHub 六格仍未触发。

## 连续完成 P2

- `recordClear` 的追加历史与 evidence stale 已在前一提交完成；本段补齐 ingest 的首次实跑与 `impact` 必填，0/非0/缺命令/缺影响四条路径都已黑盒验证。
- 新增 `iss-v2` 生命周期读取端和 quick `G-issues`：打开态最小字段、关闭态诊断字段、现存防线文件、同指纹升级与第三次 F13 候选均可机检；51 份旧 ISS 只读兼容，不倒填历史。
- RES-905/906/907 用六家官方资料收口 headless 命令；`keel/review/headless.md` 与 k-review 统一五键 pack、Finding[] 和 fail-closed 规则。本轮没有调用计费模型，live evidence 明确保留为未核实。
- 红绿：issue 状态机 1/5→5/5；headless 0/3→3/3；P2 相关 110/110；全量 237/237；`npx tsc --noEmit` exit 0。
- P6 继续只做本地等价验证，不触发真实 GitHub 六格；REQ-017/AC-4 proxy 保持 WARN。
