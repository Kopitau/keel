# 统一实施规划总览 v5

- date: 2026-09-05
- status: working（CHG-017 范围已授权，细节未宣称人工确认）
- replaces: overview-v4.md
- change: CHG-017
- requirements: v9.md
- why: 让当前指令围绕七个用户目标工作，修正自主性、澄清与完成定义，不增加流程层。

## 范围

当前任务是一次指令一致性优化，不重新实施历史 F1–F24。保留既有机器协议、运行时栈、编号和证据格式。旧功能计划只作背景；本版下表替换本轮受影响的指令与测试义务，未受影响条目继续沿用。

## 功能清单与切片 → 测试义务

| 切片 / 功能 | 修改范围 | 验证方式 |
|---|---|---|
| 需求与选型 F1/F2/F3/F4/F5 | AGENTS、grill/new/research/decide/impl；需求、功能、研究、授权连续性 | 文档一致性与独立情境审阅；不是实际模型成功率实验 |
| 功能证据 F6/F7/F9/F10 | evidence/review/accept/retro/bugfix/log；同树复用、适度测试、真实完成 | 保留 verify/trace/loop 功能回归；检查受影响文档，不新增 gate |
| 变更与交接 F11/F12/F18 | change/handoff/status、版本与范围授权 | CLI 状态：空项目、缺计划、草稿、认领/前沿、摘要、阻塞；不代替用户意图 |
| 技能与分发 F16/F21/F22/F23 | skills.ts、元数据、镜像、配置/模板、init/migrate | 调用分类、sync 重建与镜像一致、消费者 init/update 现有回归 |
| 使用说明 | README、DESIGN-v2、CONTEXT、OVERVIEW、handoff | 命令与引用可用；历史不改；已授权与已验收分别说明 |

验证中发现并修复 ISS-080：Git 索引副本时间戳导致同长度源码变更漏检。属于 F6 现有“改代码使旧证据失效”的缺陷修复，不新增接口、依赖或测试义务；定时戳回归保持原验收要求并检查用户暂存区不变。

## 接口与耦合

| ID | 从 → 到 | 本轮契约 |
|---|---|---|
| I-01 | F1 → F4/F6/F11 | 保留 REQ/AC 编号与版本，改过的工作流程不伪称人工确认 |
| I-02 | F2 → F3 | 重要决定有可检索研究；简单核验可用 worklog |
| I-05 | F4 → F6/F19 | 当前任务范围优先，前沿与 summary 不构成新授权 |
| I-07 | F7 → F8/F18 | 实现完成、正式验收和合并区分；机器判据不绕过 |
| I-11 | F16 → F17 | 一份技能源，经 sync 生成元数据与镜像 |
| I-19 | F11 → F4/F6 | 改记录不移动代码证据树；验收变化复核测试映射 |
| I-21 | F23 → F16/F21 | 安装/更新继续使用既有目录，无新依赖 |

## 实施顺序

在当前串行工作区完成：审阅与一手资料核验 → 精简指令并对齐工作版需求 → 状态/元数据修改 → 更新受影响回归 → 功能验证与独立审阅 → 更新说明交付。没有未解决的用户选择时不在阶段边界停工。

## 交付条件与非范围

说明本轮实际修复了哪些冲突；相关 CLI 和同步/安装功能检查通过，完整验证有记录，旧的真实环境替身单独披露。按技能规则进行独立只读审阅，不让同一实现上下文冒充独立结论。

不声称用户验收 v9，不为记录造新审批关卡，不改其它项目，不执行未授权外部动作。未实际运行的远端 CI、跨平台与长期模型效果不称通过。
