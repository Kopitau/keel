# keel 0.14.0 — 需求与实现治理

本轮 CHG-020 / F25，在既有记录和证据机制上加入：

- `gate drift`：读取 implementation / related 归属，比较明确复核后的需求/计划与实现内容，区分双向变化、缺失、未复核及未接入。
- `gate drift review`：以当前指纹、理由和证据路径追加内容复核历史；不改暂存区，不代替 APR、测试执行或用户验收。
- `gate atlas`：从同一模型生成离线交互总览，检索需求/功能/路径，查看技术实现、原始意图、工作解释和按类型的证据限制；无外部资源或常驻服务。
- 模板与根契约：原始意图、工作解释和技术实现分层，兼容旧扁平记录；冻结语义仍保留版本与批准规则。

## 使用与兼容

Node ≥22.18.0；没有新增 npm 依赖。旧项目可逐步声明映射，未接入不称已对齐；默认只读诊断，显式 `--check` 才严格报告待复核的已接入范围。原有八项门禁与批准哈希规则保留。

完整说明见 `tools/gate/spec-governance.md`。导出默认在 `keel/evidence/atlas.html`，是生成时快照，不是实时看板。借鉴来源与取舍见 RES-912；没有复制上游实现或引入其运行时。

## 破坏点

没有强制迁移或新增默认阻塞项；status 增加 drift 摘要行，消费脚本应按字段读取，不能依赖固定行数。只有主动声明 implementation/related 后才建立对应内容复核义务；本版本不支持 glob 或函数选择器。已有 APR 绑定语义与原八项门禁保持不变。

## 消费项目要做的事

获准升级后运行 `keel update --yes`，保留受管区外项目规则。按当前功能逐步补映射；冻结计划用新版本，不直接改其语义。首次检查为 unreviewed，核对实现与实际证据后记录理由及 fingerprint；没有复核依据时保留待复核。用 `gate atlas` 生成自己的本地总览，源码更新后重新生成。

## 验证与限制

最终命令、内容树、结果与审阅见 F25 worklog/summary；本文件不代替证据。文件哈希不能裁决语义，复核理由和真实功能证据须另行审阅。当前浏览器通道禁止直接访问本地 HTML，未绕过限制；实际客户端交互逻辑另有回归，真实浏览器布局仍待用户打开检查。远端 CI、发布、消费者升级和用户验收不从本地测试推断。
