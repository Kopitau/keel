# keel 0.12.2 — 无变化与非交互更新不再多余确认

CHG-018 的回归补漏：0.12.1 只修了 --yes 分支，普通 update 在无变化时仍会询问；非交互环境也会先显示一个无法回答的问题。

## 修复

- 无文件变化：普通 `keel update` 直接报告 `already up to date`，不询问、不取消、不写入。指令仍待迁移时保留 pending / 退出 2，不能冒充完整成功。
- 有变化、无交互终端、未带 --yes：只预览并提示 `non-interactive; rerun keel update --yes`，目标文件保持原样。
- 有变化且真正可交互：保留预览后的 y/N；--yes 仍只用于执行已授权操作。

## 破坏点

无新的退出码、依赖或授权规则。无变化和非交互分支不再输出 `Proceed? [y/N]`；机器调用方不要用这个提问是否出现来判断更新完成。完整/无变化为 0，部分完成为 2，错误为 1；未确认的取消仍为 0 且零写入。

## 消费项目要做的事

使用 0.12.2 安装器后，对需要更新的项目执行一次已授权的 `keel update --yes`。之后直接运行 `keel update`，没有变化就应显示“已是最新”，无需反复加参数或重新安装。项目专属规则和已有未提交改动的保护不变。
