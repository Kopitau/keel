interface:
  display_name: "k-grill"
  short_description: "Use when starting k-grill, interviewing the user for requirements, asking clarifying questions with recommended answers, or writing REQ entries."
policy:
  allow_implicit_invocation: true
