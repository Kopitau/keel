interface:
  display_name: "k-status"
  short_description: "Use when starting k-status, beginning a session, printing keel status, or deciding what to read next."
policy:
  allow_implicit_invocation: false
