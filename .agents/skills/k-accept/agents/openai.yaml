interface:
  display_name: "k-accept"
  short_description: "Use when starting k-accept, presenting a feature for human acceptance, or drafting an APR after review."
policy:
  allow_implicit_invocation: false
