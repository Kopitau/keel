interface:
  display_name: "k-handoff"
  short_description: "Use when starting k-handoff, ending a session, context is nearly full, or a feature just finished and keel/handoff.md must point the next context at the right files."
policy:
  allow_implicit_invocation: false
