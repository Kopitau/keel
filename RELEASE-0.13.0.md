# keel 0.13.0 — 功能证据、验收类型与当前状态

CHG-019：保留需求、充分选型、清晰代码和功能验证，修正 taotie 实际使用暴露的问题；不增加运行依赖、门禁或编排层。

## 行为变化

- 未提交的已验证内容仍能通过 G-done / X-evidence 的功能证据检查。G-merge 检查当前未提交代码，单列交付缺项并提示整理提交；提交相同内容不要求重跑。内容变化、失败或报告对账异常仍拒绝。
- gate approve 只冻结完整对账成功的证据，不再把 dirty 当失败，也不冻结损坏的 JUnit 报告。
- trace / feature_coverage 按 auto、machine-doc、manual 呈现静态名称映射。manual 始终要求另核验真实材料，测试名和 summary 不代表验收通过；ac-only 回路不能靠补名称解除人工条件。
- 已完成范围的汇总读取与 X-trace 相同的绑定计划；未批准的新草稿不静默替换既有范围。
- 普通评审入口精简，正式循环按需读取 references/formal-review.md。按变化、失败与疑点决定复验，不规定测试最多一次。
- 根指令、技能、模板与说明明确当前状态职责、局部未知的影响边界，以及研究候选与当前实施义务的区别。

## 破坏点

verify.json 原有字段保留，dirty 仍记录运行时提交状态。feature_coverage 的人话文本改为“映射”与类型说明；不要依赖旧“黑盒 N”字符串或把它当逐条运行结果。trace 增加人工证据列，完整检查可提示 manual 核验条件；该 WARN 不是自动验收成功。

## 消费项目要做的事

安装器继续支持已授权的 keel update --yes，并一并分发新参考文件。项目业务规则、历史确认件与用户未提交内容的保护不变。此次不自动修改消费者需求、计划或业务代码；需在各项目适用的任务授权下整理 living 状态。

## 验证范围

公共接口回归覆盖 dirty verify/check/同内容提交复用、失效与损坏报告、APR 快照、混合验收类型、manual ac-only、绑定计划/草稿，以及 init/update/sync 引用文件。全量与独立审阅结果以当前 handoff 和 F6 worklog 为准；本文件不代替实际运行证据、远端 CI 或用户验收。
