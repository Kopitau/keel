interface:
  display_name: "k-retro"
  short_description: "Use when starting k-retro, a feature just finished (tests green, evidence on disk), a summary must be written, OVERVIEW updated, or provisional decisions and lesson candidates closed."
policy:
  allow_implicit_invocation: false
