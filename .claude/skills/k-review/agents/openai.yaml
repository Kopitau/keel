interface:
  display_name: "k-review"
  short_description: "Use when starting k-review — the whole confirmed plan is implemented and the plan-level review loop must run — or when re-reviewing after a repair round."
policy:
  allow_implicit_invocation: false
