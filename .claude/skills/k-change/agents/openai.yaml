interface:
  display_name: "k-change"
  short_description: "Use when starting k-change, changing confirmed requirements or the unified plan, filing a CHG, or adding a feature mid-implementation."
policy:
  allow_implicit_invocation: false
